# 象棋版（chessboard / xqlink）与五子棋版（gomoku / wzqlink）对比文档

两个项目是同一套架构的两次落地：**Tauri 2 桌面壳 + Vue 3 前端 + Rust 后端**，功能都是"监听屏幕里的对局窗口 → 识别棋盘 → 本地引擎分析 → 给出行棋建议"。本文逐项对比功能、实现技术、选型原因，以及运行、打包、性能上的差异。

---

## 一、总体架构对比

| 层 | 象棋版 | 五子棋版 | 是否相同 |
|---|---|---|---|
| 桌面壳 | Tauri 2.5（Rust） | Tauri 2.5（Rust） | ✅ 相同 |
| 前端 | Vue 3.5 + TypeScript + Naive UI + Vite | 同 | ✅ 相同 |
| 窗口截图 | xcap 0.5 | xcap 0.5 | ✅ 相同 |
| 识别 | YOLO 目标检测（ONNX Runtime） | 传统 CV（校准 + 网格采样） | ❌ 不同 |
| 引擎 | Pikafish（UCI 协议） | Rapfi（piskvork 协议） | ❌ 不同（架构同构） |
| 云库 | chessdb.cn HTTP 优先 | 无 | ❌ 不同 |
| 后端进程通信 | stdin/stdout 文本协议 | stdin/stdout 文本协议 | ✅ 相同（协议内容不同） |
| 前后端事件 | `position` / `move` / `analyse` / `mirror` | `position` / `move` / `analyse` | 基本相同（少 mirror） |
| 配置/日志 | config.json + tracing 滚动日志 | 同 | ✅ 相同 |

整体复用率约 70%：窗口监听、主循环状态机骨架、配置系统、日志、前端事件协议全部同构；差异集中在**识别**和**引擎**两个"业务核心"上，而这正是两款游戏规则差异的直接映射。

---

## 二、功能逐项对比

### 1. 窗口监听与截图

| | 象棋版 | 五子棋版 |
|---|---|---|
| 实现 | `listen.rs`：xcap 枚举窗口 → 用户选窗 → `capture_image()` 整窗截图，按识别出的棋盘框裁剪 | 完全同一份代码 |

**为什么相同**：这一层与棋种完全无关——它只负责"把某个窗口的像素拿到手"。任何棋类助手、甚至非棋类场景都能复用，没有理由不同。

### 2. 棋盘识别（两个项目最大的技术分歧）

| | 象棋版 | 五子棋版 |
|---|---|---|
| 技术 | YOLO 目标检测模型（`large.onnx`），输入 640×640，15 类棋子 + 1 类棋盘框，置信度 0.7、IOU 0.5 的 NMS 后处理 | 传统 CV：用户校准两个交叉点 → 每帧对 225 个交叉点做半径 0.3 格距的圆形采样 → 量化聚类取棋盘底色 → 与底色比亮度/灰白特征判"黑/白/空" |
| 运行时 | ONNX Runtime（`ort` crate），Windows CPU 用 load-dynamic DLL，GPU 编译 CUDA/DirectML 特性，macOS CoreML，Linux CUDA | 纯 Rust 像素运算，无任何外部运行时 |
| 文件依赖 | `large.onnx` 模型 + `onnxruntime.dll` 系列 | 无（连权重文件都没有） |
| 一次性成本 | 作者需收集截图、标注、训练（不可复现的隐性成本） | 用户首次使用点两下校准（存 config，归一化坐标抗缩放/DPI） |

**为什么象棋用 YOLO**：象棋棋子有 7 种 × 2 色 = 14 类，外观是汉字刻印，不同平台字体、配色、主题各异，还会旋转/透视。这种"多类别 + 外观复杂 + 环境多变"的目标，规则写不出来，只能上深度学习检测——这也是市面上所有象棋辅助工具（如 VinXiangQi）的共同选择。

**为什么五子棋用传统 CV**：五子棋天然是"最容易识别"的棋种——
- 棋子只有黑/白两种圆片，不需要分类 14 种目标，只需要二值判断；
- 棋子永远落在规则 15×15 网格的交叉点上，几何完全已知，不需要检测"目标在哪"；
- 颜色与任何正常棋盘底色都有足够区分度（黑子极暗、白子高亮度低饱和，木纹底色蓝通道显著偏低）。

问题退化成"已知坐标处采样一个颜色再分类"，几行像素运算就能稳定解决。此时再上 YOLO 反而是负优化：要收集数据、训练模型、分发 40MB 模型文件、引入 ONNX Runtime 依赖，换来的识别质量并不更好。

**经验法则**：目标种类多、形变光照复杂 → 深度学习；目标少、几何规则、颜色可分 → 传统 CV。识别技术的选择不是偏好问题，是由**问题的信息熵**决定的。

### 3. 行棋方判定（"轮到谁走"）

| | 象棋版 | 五子棋版 |
|---|---|---|
| 实现 | 看屏幕下半边（row≥7、col 3~5）找到的是"将"还是"帅"来推断用户阵营，再由状态机逐手追踪 | 黑白子数差奇偶：黑=白 → 黑走，黑=白+1 → 白走；另有落子颜色与预期不符的奇偶校验（连续 3 次异常重置基线） |
| 为什么 | 对弈平台永远把玩家的子渲染在屏幕下半边，而"将/帅"的颜色可以直接从棋子分类读出 | 五子棋无吃子、黑先白后、子只增不减，**子数差在数学上精确等价于行棋方**，且不受中途开始监听影响 |

**为什么不能互相套用**：象棋不能用子数奇偶——吃子会破坏子数规律；五子棋不能用"屏幕下方"——棋盘无方向性，黑白子交错散布，不存在"我方的子在下半边"。同一个"判定行棋方"的需求，在两个规则体系下只有不同的最优解。

### 4. 执方（我方阵营）与镜像

| | 象棋版 | 五子棋版 |
|---|---|---|
| 镜像 | `mirror` 事件翻转棋盘显示，让用户视角与屏幕一致 | 无镜像机制——棋盘对称，翻不翻没区别 |
| 执红/执黑 | 完全自动（屏幕下方将帅颜色推断），无设置项 | 无法自动推断（棋面上没有"哪色是我"的线索），提供可选三态设置（自动/执黑/执白，存 localStorage），只影响面板"我方/对方"标注和评分着色；不设置则按行棋方视角中性显示 |

### 5. 引擎对接

| | 象棋版 | 五子棋版 |
|---|---|---|
| 引擎 | Pikafish（象棋最强开源引擎，UCI 协议，NNUE 评估） | Rapfi（Gomocup 冠军级五子棋引擎，piskvork/YIXIN 协议，mix9svq NNUE 分规则权重） |
| 摆局面 | `position fen <FEN>` | `YXBOARD` + 逐行 `x,y,颜色` + `DONE`（黑白交错发送保证行棋方奇偶正确） |
| 触发搜索 | `go depth N movetime M` | `INFO MAX_DEPTH N` + `INFO TIMEOUT_TURN M` + `YXNBEST <MultiPV>` |
| 候选招法解析 | UCI `info ... multipv k ... score cp/mate` 行 | piskvork `MESSAGE (k) <eval> | depth-seldepth | pv` 候选行 |
| 杀棋编码 | UCI `mate N` → 30000±N | `+M<p 半步数>` → 换算为整步数后同样编码 30000±N |
| 可配参数 | 深度/时间/线程/哈希/MultiPV/次优分差 | 相同六项 + 对局规则（自由/标准/连珠，影响引擎规则配置和加载的权重文件） |

**为什么架构相同**：无论什么棋，现代引擎都是"独立进程 + stdin/stdout 文本协议 + 搜索线程常驻"的形态，适配层的结构（子进程管理、参数下发、逐行解析、MultiPV 收集、超时保护）可以完全同构。

**为什么协议不同**：引擎作者不同、棋种不同，协议生态就不同——象棋世界统一在 UCI，五子棋世界统一在 piskvork（Gomocup 比赛标准）。适配层写法一致，只是"翻译的词表"不同。

**共同的取舍**：两者都不让引擎常驻思考，而是"局面变化才触发一次限时搜索"（`movetime` / `TIMEOUT_TURN`），时间到即取当前最优——这保证引擎 CPU 占用有确定上限，不会拖垮机器。

### 6. 云库

| | 象棋版 | 五子棋版 |
|---|---|---|
| 实现 | chessdb.cn HTTP 查询（`querypv`/`queryall`），命中毫秒级返回、不启动本地思考；超时或无果才落本地引擎 | 无 |

**为什么不同**：象棋有 chessdb.cn 这个公益云库（数千万局面），五子棋没有等价的公开服务。这不是技术取舍，是**生态差异**——没有云可查，功能只能整体移除，配置面板里"云库开关/超时"两项随之消失。

### 7. 记谱与棋谱显示

| | 象棋版 | 五子棋版 |
|---|---|---|
| 实现 | ICCS 坐标 → 中文纵线记谱（"车二平三""前马进七"），`chess.rs` 约 700 行，处理纵向重叠、多兵编号等特殊情况 | 直接坐标记谱 A1~O15，零特殊规则 |

**为什么**：中文记谱是象棋独有传统，"进/退/平"依赖棋子种类和方向，"前/后/1~5 号"依赖同线重叠判断。五子棋没有子种、没有移子，坐标就是唯一且完整的记谱——**代码量直接少了 700 行**，这也是两个项目后端体量差异（象棋版 server/src 约 2200 行，五子棋版约 1100 行）的主要原因。

### 8. 局面合法性校验（board_check）

| | 象棋版 | 五子棋版 |
|---|---|---|
| 实现 | 逐子检查：将帅定位、士象兵的可站位置、各类子数量上限，共约 140 行 | 一条规则：黑白子数差 ≤ 1 且黑不少于白 |

原因同上——象棋的合法性空间复杂，五子棋的合法性几乎只有"落子奇偶"一条。

### 9. 主循环状态机（worker.rs）

结构同构：`建基线 → 跟踪变化 → 落子校验（稳定确认 100ms + 合法性）→ 分析 → 推送 → 等待下一手`，含"预期棋盘"快路径（玩家按建议落子时跳过 diff）、连续 3 次无效变化重置基线。

差异点：五子棋删掉了 StartPos/阵营分支（不再需要），增加了**落子颜色奇偶校验**（防识别漏检污染引擎输入）；象棋版多一个"首次启动即分析初始局面"的特殊分支。

### 10. 前端渲染

| | 象棋版 | 五子棋版 |
|---|---|---|
| 棋盘 | `board.png` 图片做背景 + 90 个绝对定位格子 | 纯 CSS：单元格边框画 15 条横竖线，棋子是 CSS 径向渐变圆片，星位/天元用伪元素 |
| 棋子 | `piece.png` 精灵图，`background-position-y` 切 14 种棋子 | 黑/白两个 CSS 类 |
| 背景主题 | `bg.jpg` 浅枫木照片铺满全窗，面板透明 | 同一张 `bg.jpg`（直接复用素材），同款透明面板风格 |

**为什么**：14 种带汉字的棋子必须用图片（精灵图是最省事的方案）；两种颜色的圆片 CSS 渐变即可，比切图更灵活（改色/调大小不用动素材）。背景主题直接复用象棋版素材，保证两个工具视觉统一。

### 11. 引擎配置面板

| 配置 | 象棋版 | 五子棋版 |
|---|---|---|
| 深度 / 时间 / 线程 / 哈希 / 候选招数 / 次优分差 | ✅ | ✅（语义一致） |
| 云库开关 / 云库超时 | ✅ | ❌（无云库） |
| 对局规则 | ❌（象棋规则唯一） | ✅ 自由/标准/连珠（映射 Rapfi 的 RULE 0/1/2，并联动权重文件） |
| 生效时机 | 线程/哈希需重启应用 | 线程/哈希/规则修改后**自动调 `reload_engine` 立即生效**（改进项） |

---

## 三、运行（dev 模式）的区别

| | 象棋版 | 五子棋版 |
|---|---|---|
| 启动命令 | `pnpm tauri dev` | `pnpm tauri dev`（相同） |
| 首次启动准备 | ① 手动建 `target\debug\_up_` junction 指向 `libs/`（否则资源路径找不到，panic）② 复制 `libs/windows-cpu/*.dll` 到 `target\debug\`（否则 ort 按目录搜索 DLL 可能命中系统里旧版，版本校验 panic） | 无。tauri-build 会自动把 resources 声明的引擎文件复制到 `target\debug\_up_\` |
| 失败表现 | 引擎路径 panic / ort 加载 panic | 一般无（除非删了 libs/rapfi） |
| 识别前提 | 无（模型全自动） | 首次需"校"：选窗口 → 在截图上点 A1、O15 两个交叉点 |

**为什么象棋 dev 更麻烦**：它的运行时依赖（模型、ONNX DLL）散落在资源目录和 exe 目录两处，旧版 tauri-build 的 dev 资源映射不完整才需要 junction 手工兜底；ONNX Runtime 以 load-dynamic 方式加载，dev 下不自动放 DLL。五子棋版没有任何外部运行时，资源又全部通过 `bundle.resources` 声明，tauri-build 自动就位——**dev 体验的差异本质上是"外部依赖数量"的差异**。

---

## 四、打包的区别

| | 象棋版 | 五子棋版 |
|---|---|---|
| 命令 | `pnpm build:cpu` / `pnpm build:gpu`（两个变体） | `pnpm build:msi`（单一变体） |
| 产物 | `xqlink_0.1.2_x64_en-US.msi`，CPU/GPU **文件名相同互相覆盖**，打完要手动加后缀 | `wzqlink_0.1.0_x64_en-US.msi` |
| 体积 | CPU 约 75MB；GPU 约 350MB+ | 约 50MB |
| GPU 权重 | 196MB 的 `windows-gpu.zip` 不在仓库（超 GitHub 100MB 限制），打 GPU 包前要手动下载解压 | 不存在此问题 |
| 交替打包污染 | GPU 打包残留的 CUDA DLL（320MB）会被下次 CPU 包扫进安装包（75MB 变 266MB），必须先清理 `target\release` | 无 DLL，无污染可能 |
| GPU 加速 | DirectML / CUDA / CoreML | 无 GPU 路径 |

**为什么**：象棋的 YOLO 推理计算量大，GPU 加速有实际收益，所以才需要 CPU/GPU 双变体，而双变体+共享 target 目录又催生了污染问题。五子棋的识别是几百个小区域的均值采样，纯 CPU 微秒到毫秒级完成，GPU 毫无用武之地——**没有需求就没有变体**，打包流程反而更简单可靠。

---

## 五、性能对比

### 1. 监听态（常驻开销，两者每 200ms 截图一帧）

| | 象棋版 | 五子棋版 |
|---|---|---|
| 每帧识别成本 | 一次 YOLO 前向推理：640×640 输入、4 个推理线程，CPU 模式下每帧占用多核数毫秒到数十毫秒；≈ 每秒 5 帧，是**持续的多核占用** | 225 个交叉点采样 + 底色聚类：单核亚毫秒到 1ms 级 |
| 换算 | 每帧 ≈ 数十 ms×多核 | 每帧 ≈ <1ms×单核 |
| 量级差 | —— | 常驻 CPU 占用低 **1~2 个数量级** |

象棋版的项目待办里专门列了"帧差跳帧：先做廉价像素 diff 跳过 YOLO 推理，预计监听基础 CPU 再降 50%+"——这条优化恰恰证明了 YOLO 推理就是它监听态的瓶颈。五子棋版天然没有这个瓶颈，识别本身比"判断要不要识别"还便宜。

**推理线程对比**：象棋版监听期间 ort 的 4 个推理线程是主要负载；五子棋版监听期间几乎没有线程负载，引擎线程数配置（默认 4）只在分析瞬间生效。

### 2. 引擎思考态（瞬时开销，受配置时间上限约束）

| | 象棋版（Pikafish） | 五子棋版（Rapfi） |
|---|---|---|
| 评估方式 | NNUE（7×7 网格特征） | NNUE（分规则权重，文件约 10MB×4） |
| 搜索形态 | alpha-beta + MultiPV，分支因子大（几十个合法着法）、需要审克制/将杀等复杂规则 | alpha-beta + MultiPV，合法点最多 225 个但候选剪枝强，局面评估更轻 |
| CPU 占用 | 满配置线程数（默认 4 线程跑满对应核心），持续到时间上限（默认 5s） | 同样满线程数跑满，默认时间上限 3s |
| 占用可控性 | 完全由"时间 × 线程"两个配置决定 | 同左 |

两者引擎思考时都是** CPU 密集型短跑**，峰值占用取决于线程配置而非棋种；五子棋由于分支因子和评估更轻，同线程数的每秒节点数（NPS）通常更高、同样时间内搜索深度更深，但这只是"同样的 CPU 预算买到更深的计算"，**峰值功耗两者相当**。

### 3. 内存

| | 象棋版 | 五子棋版 |
|---|---|---|
| 置换表 | 默认 64MB（可配至 102400MB） | 相同 |
| 模型/权重 | ONNX 模型驻留 + ort 会话开销 | NNUE 权重约 40MB |
| 量级 | 相近，都在 100~200MB 进程内存水平 | 相近 |

### 4. 磁盘与安装

| | 象棋版 | 五子棋版 |
|---|---|---|
| 仓库体积 | 模型 40MB 级（GPU 权重另发 Release） | 引擎+权重约 40MB |
| 安装包 | 75MB / 350MB+ | 约 50MB |
| GPU 驱动依赖 | GPU 版需要 N 卡驱动，无驱动时按注册顺序回落 DirectML/CPU | 无 |

### 5. 一句话性能结论

**监听态五子棋显著更省 CPU**（传统 CV 对 YOLO 是 1~2 个量级的差距，象棋版还在待办里挂着的"帧差跳帧"优化对五子棋来说天生免费）；**引擎思考态两者相当**（同为 NNUE 短跑，峰值都由线程数决定）；**内存和磁盘相近**。若两开挂机，五子棋版对笔记本续航和风扇明显更友好。

---

## 六、总结：哪些相同是"应该的"，哪些不同是"必然的"

- **应该相同的**（与棋种无关的工程层）：桌面壳、截图、进程管理、配置、日志、前端框架与事件协议、状态机骨架——这 70% 是通用"棋类助手框架"，复用它们是两次开发最大的收益。
- **必然不同的**（由游戏规则决定的业务层）：
  - 识别：14 类汉字棋子 → 深度学习；2 色圆子 + 规则网格 → 传统 CV；
  - 行棋方：吃子破坏奇偶 → 空间推断；无吃子 → 数学奇偶；
  - 记谱：中文纵线进退 → 700 行规则；纯落子 → 坐标即可；
  - 引擎：棋种决定引擎，引擎决定协议词表；
  - 云库：生态有无决定功能去留；
  - GPU/打包：识别算力需求决定是否存在双变体及其连带问题。
- **一个技术判断可以带走**：选型不追"先进"，追"问题规模"。当目标检测问题退化成"已知坐标处的二值分类"时，把 YOLO 换成几行像素运算，省下的是模型文件、运行时依赖、GPU 变体、DLL 分发、打包清理脚本和常驻功耗——**五子棋版所有的简化都源自这一条**。
